# 💻 Практичне завдання 3: Laravel та Symfony API

Цей репозиторій містить виконання лабораторної/практичної роботи зі створення API за допомогою двох популярних PHP-фреймворків: **Laravel** та **Symfony**. Також включено колекції запитів для тестування створених ендпоінтів.

## 📁 Структура проєкту

- 📂 `laravel/` — бекенд-частина, реалізована на фреймворку Laravel.
- 📂 `symfony/` — бекенд-частина, реалізована на фреймворку Symfony.
- 📂 `postman/` (та `.postman/`) — експортовані колекції Postman для перевірки працездатності API.

---

## ⚙️ Встановлення та запуск

Для локального розгортання проєктів переконайтеся, що у вас встановлені PHP, [Composer](https://getcomposer.org/) та [Postman](https://www.postman.com/).

### 1. Налаштування Laravel API
Перейдіть у директорію Laravel та встановіть необхідні пакети:
```bash
cd laravel
composer install
## 🛠️ Підтвердження реалізації бeкенду на Laravel

Для розробки серверної частини проєкту було використано фреймворк **Laravel**. Нижче наведено структуру репозиторію та результати тестування працездатності створеного API.

## 🛠️ Підтвердження реалізації бекенду на Laravel

Для розробки серверної частини проєкту було використано фреймворк **Laravel**. Нижче наведено структуру репозиторію та результати тестування працездатності створеного API.

### 1. Структура проєкту
У корені репозиторію виділено окрему директорію `laravel/`, яка містить усю логіку фреймворку, конфігурації та інструкції з розгортання:

<img width="900" alt="Структура репозиторію з Laravel" src="https://github.com/user-attachments/assets/f4daa9c0-2ac1-4d72-9526-5a6883b369bd" />

---

### 2. Тестування працездатності API (CRUD операції в Postman)
Для перевірки коректності роботи створеного Laravel-застосунку було налаштовано та протестовано колекцію запитів **"Recipe API (Laravel)"**. Усі ендпоінти повертають відповідні статус-коди та структуровані JSON-дані.

#### 🟢 Отримання списку всіх записів (GET)
Запит до `GET {{base_url}}/api/recipes` повертає повний масив об'єктів із бази даних. Статус відповіді: **200 OK**.

<img width="908" height="926" alt="GET Request - Отримання списку" src="https://github.com/user-attachments/assets/f4daa9c0-2ac1-4d72-9526-5a6883b369bd" />

#### 🔵 Створення нового запису (POST)
Надсилання даних через `POST {{base_url}}/api/recipes` успішно створює новий об'єкт у базі. Статус відповіді: **201 Created**.

<img width="959" height="900" alt="POST Request - Створення запису" src="https://github.com/user-attachments/assets/6de97ce5-dd1a-4e4f-a073-b946396314a3" />

#### 🟡 Отримання детальної інформації за ID (GET)
Запит `GET {{base_url}}/api/recipes/{{recipe_id}}` повертає конкретну сутність за її ідентифікатором. Статус відповіді: **200 OK**.

<img width="941" height="954" alt="GET Request - Отримання за ID" src="https://github.com/user-attachments/assets/756c5558-b5be-4248-b6be-6e36b3332531" />

#### 🟠 Оновлення існуючих даних (PATCH)
Запит `PATCH {{base_url}}/api/recipes/{{recipe_id}}` успішно модифікує вказані поля (наприклад, зміна назви на "Борщ оновлений"). Статус відповіді: **200 OK**.

![Скріншот видалення](scr/laravel/del.png)

---
Виконані тести повністю підтверджують успішну інтеграцію, працездатність маршрутів (routing) та взаємодію з базою даних у межах фреймворку Laravel.
<img width="901" height="904" alt="patch" src="https://github.com/user-attachments/assets/284796a2-9089-44cb-ae66-4f55c3375617" />

---
Виконані тести повністю підтверджують успішну інтеграцію, працездатність маршрутів (routing) та взаємодію з базою даних у межах фреймворку Laravel.

